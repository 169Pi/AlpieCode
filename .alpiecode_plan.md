# AlpieCode Task Plan

**Deliverables:**
1. `math_utils.py` - contains a fibonacci function
2. `test_math_utils.py` - contains pytest tests for the fibonacci function

**Files to change:**
- Create `math_utils.py` with a fibonacci function that returns the nth Fibonacci number
- Create `test_math_utils.py` with pytest tests covering edge cases and normal cases

**Verification checks:**
- The fibonacci function returns correct values for n=0, 1, 2, 5, 10
- All pytest tests pass with 0 failures
- The script runs without errors
