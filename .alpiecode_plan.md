# AlpieCode Task Plan

**Deliverables:**
1. `calculator.py` - A Python CLI calculator with arithmetic operations
2. `test_calculator.py` - Comprehensive unit tests

**Files to create:**
- `calculator.py`: Main calculator module with:
  - Addition, subtraction, multiplication, division
  - Input validation and error handling
  - Command-line interface
  - Proper type hints and docstrings

- `test_calculator.py`: Unit tests covering:
  - All arithmetic operations (normal cases)
  - Edge cases (zero, negative numbers, decimals)
  - Error cases (division by zero, invalid input)
  - CLI interface tests

**Verification checks:**
- Calculator compiles without errors
- All unit tests pass (pytest)
- CLI works with various inputs
- No warnings when running with -Wall -Wextra
